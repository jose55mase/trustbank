# Configuración para desarrollo local
server.port=8081
spring.application.name=bank

# Base de datos local
spring.datasource.url=jdbc:mysql://localhost:3306/guardianstrustbank?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.password=jose595511
spring.datasource.username=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# CORS para desarrollo
#cors.allowed-origins=http://localhost:4200,http://localhost:8080,http://localhost:3000

# Archivos
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# SSL deshabilitado para desarrollo
server.ssl.enabled=false

# Logging
logging.level.org.hibernate.SQL=debug
logging.level.org.springframework.web=debug