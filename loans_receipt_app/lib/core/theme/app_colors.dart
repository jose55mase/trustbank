import 'package:flutter/material.dart';

class AppColors {
  // Colores principales - Paleta elegante azul oscuro y dorado
  static const  primary = Color(0xFF1A237E); // Azul índigo profundo
  static const primaryLight = Color(0xFF534BAE);
  static const primaryDark = Color(0xFF000051);
  
  static const secondary = Color(0xFFFFB300); // Dorado elegante
  static const secondaryLight = Color(0xFFFFE54C);
  static const secondaryDark = Color(0xFFC68400);
  
  static const accent = Color(0xFF00BFA5); // Verde azulado
  static const accentLight = Color(0xFF5DF2D6);
  
  static const error = Color(0xFFD32F2F);
  static const warning = Color(0xFFFFA726);
  static const success = Color(0xFF66BB6A);
  static const info = Color(0xFF42A5F5);
  
  // Fondos y superficies
  static const background = Color(0xFFF5F7FA);
  static const surface = Colors.white;
  static const surfaceVariant = Color(0xFFFAFBFC);
  
  // Textos
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF757575);
  static const textHint = Color(0xFFBDBDBD);
  
  // Bordes y divisores
  static const border = Color(0xFFE0E0E0);
  static const divider = Color(0xFFEEEEEE);
  
  // Gradientes
  static const gradientStart = Color(0xFF1A237E);
  static const gradientEnd = Color(0xFF534BAE);
}
